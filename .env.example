# Optional. The buildings, trees, and permits layers are all PUBLIC, so the app
# runs with no key. Providing an ArcGIS API key only upgrades the basemap/ground
# to Esri's hosted dark 3D basemap. Get one free at https://developers.arcgis.com
VITE_ARCGIS_API_KEY=
